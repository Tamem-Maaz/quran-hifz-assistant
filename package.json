{
  "name": "quran-hifz-assistant",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "مساعد شخصي لحفظ القرآن الكريم — يقود جلسة الحفظ اليومية ويجدول المراجعة تلقائيًا",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit --checkJs",
    "e2e": "playwright test"
  },
  "devDependencies": {
    "@eslint/js": "^9.12.0",
    "@playwright/test": "^1.48.0",
    "@vitest/coverage-v8": "^2.1.0",
    "eslint": "^9.12.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
