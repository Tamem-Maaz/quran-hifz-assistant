/**
 * إصدار التطبيق المعروض في الإعدادات (القسم 12) — يُحدَّث يدويًا مع `version` في
 * package.json عند كل إصدار، إذ لا توجد أداة بناء تُوحّدهما تلقائيًا (القرار 1).
 */
export const APP_VERSION = "0.1.0";
